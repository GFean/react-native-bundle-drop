'use strict';

const ANDROID_ENABLED_META_DATA = 'com.bundledrop.EXPO_ENABLED';
const IOS_ENABLED_INFO_PLIST_KEY = 'BundleDropExpoEnabled';

module.exports = {
  ANDROID_ENABLED_META_DATA,
  IOS_ENABLED_INFO_PLIST_KEY,
};
